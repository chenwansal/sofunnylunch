{
  "name": "ourlunch",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "npm run cli && npm run server",
    "cli": "cd ./view && tsc && webpack",
    "server": "tsc && node ./dist/App.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "axios": "^0.24.0",
    "jackwebutil": "^1.5.0"
  }
}
